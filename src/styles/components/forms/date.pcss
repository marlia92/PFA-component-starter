.date {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-xs);
  width: 100%;
  margin: 0;
  margin-bottom: var(--spacing-md);

  .required {
    color: red;
    margin-inline-end: var(--spacing-em-xs);
  }
}
