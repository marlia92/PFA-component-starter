---
const { contentBlocks } = Astro.props;
---

<main>
  {
    renderContentBlocks(contentBlocks).map((item) => {
      if (item) {
        const { Component, props } = item;

        return <Component {...props} />;
      }
      return null;
    })
  }
</main>
